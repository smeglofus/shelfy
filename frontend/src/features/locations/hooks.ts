// Locations feature hooks – re-exported from the original shared hooks file.
export {
  useLocations,
  useCreateLocation,
  useUpdateLocation,
  useDeleteLocation,
} from '../../../hooks/useLocations'
